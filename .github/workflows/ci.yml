name: Node.js CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  DATABASE_URL: postgres://postgres:A5incH1red@codeworks-thesis-asynchired.czsyoams2vef.eu-west-1.rds.amazonaws.com:5432/initial_db
  NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_c2hhcnAtdHVya2V5LTEyLmNsZXJrLmFjY291bnRzLmRldiQ
  CLERK_SECRET_KEY: sk_test_JlIO4hDwWD7PpluNZ4cPfEKzx2c3N6ngtmDHlejpS0

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.9.0

      - name: Install dependencies
        run: npm install

      - name: TypeScript Type Checking
        run: npm run typecheck

      - name: Linting
        run: npm run lint

      - name: Building
        run: npm run build
